class Session {
  final String consumerKey;
  final String consumerSecret;
  final String accessToken;
  final String accessSecret;

  Session(this.consumerKey, this.consumerSecret, this.accessToken, this.accessSecret);
}