import 'package:flutter/material.dart';

class Lemonade extends StatelessWidget {
  @override
  Widget build(BuildContext context) => MaterialApp(
        title: 'Lemonade',
        home: Scaffold(),
      );
}
